# Base de datos

- Las migraciones reales de Cloudflare D1 están en `apps/api/migrations`.
- El seed inicial está en `database/seed.sql`.
- El seed trae equipos/fixture como placeholders editables desde el panel admin para no depender de APIs pagas ni scraping.

Usuario admin inicial local:

- Email: `admin@prode.local`
- Contraseña: `admin12345`

Cambiar la contraseña apenas se despliegue.
